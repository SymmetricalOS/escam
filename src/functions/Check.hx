package functions;

class Check {
    // returns a list of conflicting pkgs
    public static function pkgConflicts(pkg:String):Array<String> { return []; }

    public static function fileConflicts(pkg:String):Array<String> { return []; }
}
